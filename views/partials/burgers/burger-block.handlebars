<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}Eat!{{else}}Order Another!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Remove!</button>
</li>
